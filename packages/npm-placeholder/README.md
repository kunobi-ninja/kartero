# `@kunobi/kartero` registry bootstrap

This placeholder only creates the npm package so its Trusted Publisher can be
configured. Install a stable version instead.

Project: https://github.com/kunobi-ninja/kartero
